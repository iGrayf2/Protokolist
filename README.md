# Protokolist

Protokolist — локальный помощник для расшифровки аудиозаписей совещаний и подготовки материалов для корпоративного протокола.

Главная цель проекта — получить максимально качественный сырой материал из аудио, чтобы потом на его основе делать осмысленную стенограмму, протокол, решения и задачи.

Первая версия делает главное:

- расшифровывает MP3/WAV/M4A/OGG/FLAC/MP4/WEBM через Faster-Whisper;
- работает в режиме максимального качества по умолчанию;
- сохраняет богатый сырой JSON с таймкодами сегментов, словами и метаданными распознавания;
- сохраняет сырой TXT без правок словарем;
- сохраняет очищенные TXT/SRT/DOCX;
- создает `*_protocol_prompt.md` — готовый промпт для ChatGPT с участниками и стенограммой.

## Статус проекта

Текущая версия: `0.2.0`.

Это ранняя рабочая версия. Главная цель — стабильно получить качественный текст из записи совещания на Windows-ноутбуке без ручных команд.

## Требования

- Windows 10/11
- Python 3.11 или 3.12
- Интернет при первом запуске, чтобы скачать зависимости и модель Whisper
- 16 ГБ RAM рекомендуется для режима `large-v3/int8`

Для Lenovo IdeaPad Slim 5 14AHP9 / Ryzen 5 8645HS / 16 GB RAM рекомендуемый режим:

- пресет: `Максимум качества`
- модель: `large-v3`
- язык: `ru`
- compute type: `int8`

Этот режим медленнее, но дает лучший сырой материал для последующей обработки в ChatGPT.

## Установка на Windows

Открой PowerShell или CMD и выполни:

```bat
git clone https://github.com/iGrayf2/Protokolist.git
cd Protokolist
scripts\install_windows.cmd
```

После установки запуск GUI:

```bat
scripts\run_gui.cmd
```

## Быстрый запуск через перетаскивание файла

После установки можно перетащить аудиофайл на:

```text
scripts\transcribe_file.cmd
```

Результаты появятся в папке:

```text
output\
```

## Что получается на выходе

Для файла `meeting.mp3` будут созданы:

```text
output\meeting.raw.json
output\meeting.raw.txt
output\meeting.cleaned.txt
output\meeting.cleaned.srt
output\meeting.cleaned.docx
output\meeting_protocol_prompt.md
```

### Главное отличие raw и cleaned

`meeting.raw.json` и `meeting.raw.txt` — это сырой результат распознавания без словарных замен. Эти файлы нужны как главный источник правды.

`meeting.cleaned.*` — это уже очищенная версия, где применен словарь типовых исправлений: 1С, Честный Знак, имена участников и производственные термины.

Файл `meeting_protocol_prompt.md` можно отправить в ChatGPT для формирования протокола совещания.

## Участники по умолчанию

Список постоянных участников хранится в файле:

```text
participants.txt
```

Его можно редактировать обычным Блокнотом.

## Словарь исправлений

Словарь терминов хранится в файле:

```text
config\dictionary.json
```

Он используется двумя способами:

- как подсказка для Whisper перед распознаванием;
- как мягкая очистка текста после сохранения raw-файлов.

## Архитектура

```text
Protokolist/
├── run.py
├── requirements.txt
├── participants.txt
├── config/
│   └── dictionary.json
├── scripts/
│   ├── install_windows.cmd
│   ├── run_gui.cmd
│   └── transcribe_file.cmd
└── src/
    └── protokolist/
        ├── app.py
        ├── cleanup.py
        ├── cli.py
        ├── exporters.py
        ├── protocol_prompt.py
        ├── raw_exporters.py
        ├── segments.py
        └── transcriber.py
```

## Дорожная карта

### v0.1

- [x] GUI
- [x] CLI
- [x] Faster-Whisper
- [x] TXT export
- [x] SRT export
- [x] DOCX export
- [x] prompt export for ChatGPT

### v0.2

- [x] режим максимального качества по умолчанию
- [x] word timestamps
- [x] сырой JSON
- [x] сырой TXT без очистки
- [x] раздельные cleaned-файлы
- [x] единая cleanup-логика в GUI и CLI
- [ ] нормальный прогресс по длительности аудио
- [ ] настройка папки вывода
- [ ] сохранение настроек
- [ ] обработка длинных записей стабильными чанками
- [ ] проверка установленного Python и понятные сообщения об ошибках

### v0.3

- [ ] предобработка аудио через ffmpeg: mono WAV 16 kHz, нормализация громкости
- [ ] автоматическая сборка `Protokolist.exe`
- [ ] иконка приложения
- [ ] drag and drop в окно
- [ ] история обработанных совещаний

### v0.4

- [ ] диаризация говорящих
- [ ] роли участников
- [ ] словарь терминов предприятия
- [ ] автоподстановка типовых участников

### v0.5

- [ ] генерация протокола через LLM
- [ ] экспорт готового протокола в DOCX/PDF
- [ ] список решений
- [ ] Action Items

## Важное ограничение

Faster-Whisper хорошо распознает речь, но сам по себе не определяет надежно, кто именно говорит. В текущей версии файл `*_protocol_prompt.md` помогает ChatGPT сопоставлять реплики с участниками по контексту. Настоящая диаризация будет добавлена отдельным модулем позже.
